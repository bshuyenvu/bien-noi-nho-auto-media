services:
  auto-media:
    build: .
    container_name: bien-noi-nho-auto-media
    restart: unless-stopped
    ports:
      - "8787:8787"
    environment:
      PORT: 8787
      RENDER_API_KEY: ${RENDER_API_KEY:-}
    volumes:
      - ./data:/app/data
      - ./output:/app/output

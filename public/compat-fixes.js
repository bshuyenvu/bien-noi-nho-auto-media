(()=>{const $=id=>document.getElementById(id),nativeFetch=window.fetch.bind(window);let lastMedia=[];window.fetch=async(input,init={})=>{const url=typeof input==='string'?input:input?.url||'';const r=await nativeFetch(input,init);if(url==='/api/media-studio'&&r.ok){try{const j=await r.clone().json();lastMedia=Array.isArray(j.candidates)?j.candidates:[];setTimeout(patchMediaThumbs,0)}catch{}}return r};function patchMediaThumbs(){const cards=[...document.querySelectorAll('#mediaGrid .media-card')];cards.forEach((c,i)=>{const img=c.querySelector('img'),m=lastMedia[i];if(img&&m?.previewData&&img.src!==m.previewData){img.src=m.previewData;img.onerror=()=>{img.removeAttribute('src');img.alt='Không hiển thị được ảnh preview'}}})}function hideLegacyDuplicates(){const cards=[...document.querySelectorAll('.card')];for(const c of cards){const h=c.querySelector(':scope>h2')?.textContent?.trim();if((h==='RSS'||h==='Tin RSS')&&$('rssAdmin'))c.style.display='none'}const badge=document.querySelector('.badge');if(badge)badge.textContent='STABILITY SYNC'}function verifyModules(){const required=[['rssAdmin','RSS Admin'],['reviewWorkspace','Review'],['productionQueue','Production Queue'],['renderMonitor','Render Monitor'],['workflowNav','Workflow']];const missing=required.filter(([id])=>!$(id)).map(([,name])=>name);if(missing.length)console.warn('Auto Media modules not mounted:',missing.join(', '))}function mount(){hideLegacyDuplicates();const grid=$('mediaGrid');if(grid)new MutationObserver(()=>setTimeout(patchMediaThumbs,0)).observe(grid,{childList:true,subtree:true});setTimeout(verifyModules,1800);setTimeout(hideLegacyDuplicates,1200)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',mount);else mount()})();
(()=>{const $=id=>document.getElementById(id),nativeFetch=window.fetch.bind(window);let state={draftId:null,status:'needs_review',locks:{script:false,media:false,voice:false,scenes:false}};function auth(){const k=sessionStorage.getItem('renderApiKey')||'';return k?{authorization:'Bearer '+k}:{}}async function api(path,method='GET',body){const r=await nativeFetch(path,{method,headers:{...auth(),...(body?{'content-type':'application/json'}:{})},body:body?JSON.stringify(body):undefined}),j=await r.json();if(!r.ok)throw Error(j.error||r.status);return j}function mount(){const anchor=$('autoProducer')||document.querySelector('.hero');if(!anchor||$('reviewWorkspace'))return;const box=document.createElement('section');box.id='reviewWorkspace';box.className='media-studio';box.innerHTML=`<h3 style="margin:0">✅ Review & Approval Workspace</h3><div class="muted">Khóa đủ Kịch bản • Ảnh • Giọng đọc • Scene rồi duyệt. Backend sẽ chặn render nếu chưa hoàn tất Review.</div><div id="reviewState" class="voice-meta">Chưa có bản tin để duyệt.</div><div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(170px,1fr));gap:8px"><label><input id="lockScript" type="checkbox" style="width:auto"> 🔒 Kịch bản</label><label><input id="lockMedia" type="checkbox" style="width:auto"> 🔒 Ảnh</label><label><input id="lockVoice" type="checkbox" style="width:auto"> 🔒 Giọng đọc</label><label><input id="lockScenes" type="checkbox" style="width:auto"> 🔒 Scene</label></div><div style="display:flex;gap:8px;flex-wrap:wrap"><button id="needsFixBtn" class="secondary">✏️ CẦN SỬA</button><button id="approveReviewBtn">✅ DUYỆT BẢN TIN</button></div>`;anchor.insertAdjacentElement('afterend',box);['Script','Media','Voice','Scenes'].forEach(k=>{$('lock'+k).onchange=e=>{state.locks[k.toLowerCase()]=e.target.checked;if(state.status==='approved'||state.status==='ready')state.status='needs_review';sync()}});$('needsFixBtn').onclick=()=>setStatus('needs_fix');$('approveReviewBtn').onclick=approve;document.addEventListener('auto-producer-ready',e=>setDraft(e.detail?.draftId||null));render()}function allLocked(){return Object.values(state.locks).every(Boolean)}function render(){const el=$('reviewState');if(!el)return;const names={needs_review:'🟡 CẦN DUYỆT',needs_fix:'🔴 CẦN SỬA',approved:'🟢 ĐÃ DUYỆT',ready:'🟢 SẴN SÀNG RENDER'};el.textContent=`${names[state.status]||state.status} • ${Object.values(state.locks).filter(Boolean).length}/4 thành phần đã khóa${state.draftId?' • Draft '+state.draftId.slice(0,8):''}`;$('approveReviewBtn').disabled=!state.draftId||!allLocked()}async function load(){if(!state.draftId)return;try{const x=await api(`/api/drafts/${state.draftId}/review`);if(x.review){state.status=x.review.status;state.locks=x.review.locks||state.locks;for(const [k,v] of Object.entries(state.locks)){const el=$('lock'+k[0].toUpperCase()+k.slice(1));if(el)el.checked=Boolean(v)}render()}}catch(e){$('reviewState').textContent='⚠️ '+e.message}}async function sync(){render();if(!state.draftId)return;try{const x=await api(`/api/drafts/${state.draftId}/review`,'POST',{status:state.status,locks:state.locks});state.status=x.review?.status||state.status;render();document.dispatchEvent(new CustomEvent('review-updated',{detail:x}))}catch(e){$('reviewState').textContent='⚠️ '+e.message}}async function setStatus(status){state.status=status;await sync()}async function approve(){if(!state.draftId||!allLocked())return;state.status='approved';await sync()}async function setDraft(id){state={draftId:id,status:'needs_review',locks:{script:false,media:false,voice:false,scenes:false}};for(const k of ['Script','Media','Voice','Scenes']){const el=$('lock'+k);if(el)el.checked=false}render();await load()}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',mount);else mount();window.ReviewWorkspace={setDraft,get state(){return state},reload:load}})();
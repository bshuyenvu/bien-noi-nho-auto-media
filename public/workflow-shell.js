(()=>{const $=id=>document.getElementById(id);function css(){if($('workflowShellStyle'))return;const s=document.createElement('style');s.id='workflowShellStyle';s.textContent=`.wf-nav{position:sticky;top:0;z-index:30;margin:14px 0 18px;padding:9px;background:#0b1627ee;border:1px solid #263449;border-radius:14px;display:flex;gap:7px;flex-wrap:wrap;backdrop-filter:blur(10px)}.wf-nav button{margin:0;background:#17243a;color:#cbd5e1}.wf-nav button.active{background:#2563eb;color:#fff}.wf-stage{display:none}.wf-stage.active{display:block}.wf-stage>.card,.wf-stage>.grid,.wf-stage>.media-studio,.wf-stage>.sys-grid{margin-top:14px}.wf-stage>.grid{display:grid}.wf-head{display:flex;justify-content:space-between;gap:10px;align-items:center;margin:4px 0 10px}.wf-title{font-size:21px;font-weight:850}.wf-note{color:#94a3b8;font-size:13px}.wf-flow{display:flex;gap:5px;flex-wrap:wrap;color:#93c5fd;font-size:12px}.wf-flow span{padding:4px 8px;background:#0f2038;border-radius:999px}.wf-settings .card{max-width:760px}.wf-hidden{display:none!important}@media(max-width:820px){.wf-nav{position:static}.wf-stage>.grid{grid-template-columns:1fr}}`;document.head.append(s)}function cardByTitle(t){return[...document.querySelectorAll('.card')].find(c=>c.querySelector(':scope>h2')?.textContent?.trim()===t)}function move(el,host){if(el&&host&&!host.contains(el))host.append(el)}function stage(id,title,note){const s=document.createElement('section');s.id=id;s.className='wf-stage';s.innerHTML=`<div class="wf-head"><div><div class="wf-title">${title}</div><div class="wf-note">${note}</div></div><div class="wf-flow"><span>Nguồn</span><span>→ Biên tập</span><span>→ Sản xuất</span><span>→ Duyệt</span><span>→ Render</span></div></div>`;return s}function activate(id){document.querySelectorAll('.wf-stage').forEach(x=>x.classList.toggle('active',x.id===id));document.querySelectorAll('.wf-nav [data-stage]').forEach(x=>x.classList.toggle('active',x.dataset.stage===id));sessionStorage.setItem('autoMediaStage',id);scrollTo({top:0,behavior:'smooth'})}function organize(){const source=$('wf-source'),editor=$('wf-editor'),prod=$('wf-production'),review=$('wf-review'),out=$('wf-output'),settings=$('wf-settings');if(!source)return;move(cardByTitle('Nhập bài báo từ URL'),source);move($('rssAdmin'),source);move(cardByTitle('Biên tập bản tin'),editor);move($('autoProducer'),editor);const media=$('mediaAnalyze')?.closest('.media-studio'),voice=$('voiceMode')?.closest('.voicebox');[media,$('sceneStudio'),voice,$('themeStudio')].forEach(x=>move(x,prod));move($('reviewWorkspace'),review);move(cardByTitle('Bản tin'),out);move($('productionQueue'),out);const renderCard=[...document.querySelectorAll('.card')].find(c=>/Render Monitor|^Render$/.test(c.querySelector('h2')?.textContent?.trim()||''));move(renderCard,out);move($('systemCockpit'),settings);move($('apiKey')?.closest('.card'),settings);const oldRss=cardByTitle('RSS'),oldItems=cardByTitle('Tin RSS');oldRss?.classList.add('wf-hidden');oldItems?.classList.add('wf-hidden');document.querySelectorAll('.grid').forEach(g=>{if(!g.children.length)g.remove()})}function mount(){if($('workflowNav'))return;css();const main=document.querySelector('main.wrap'),hero=document.querySelector('.hero');if(!main||!hero)return;const nav=document.createElement('nav');nav.id='workflowNav';nav.className='wf-nav';[['wf-source','① Nguồn tin'],['wf-editor','② Biên tập'],['wf-production','③ Sản xuất'],['wf-review','④ Duyệt'],['wf-output','⑤ Render'],['wf-settings','⚙ Cài đặt']].forEach(([id,label])=>{const b=document.createElement('button');b.dataset.stage=id;b.textContent=label;b.onclick=()=>activate(id);nav.append(b)});hero.insertAdjacentElement('afterend',nav);main.append(stage('wf-source','Nguồn tin','Nhập URL hoặc quản lý RSS. Chọn một tin để chuyển sang Biên tập.'),stage('wf-editor','Biên tập bản tin','Chỉnh tiêu đề, kịch bản và thông tin nguồn trước khi sản xuất.'),stage('wf-production','Sản xuất video','Ảnh → Scene → Voice → Theme. Kiểm tra kỹ trước khi duyệt.'),stage('wf-review','Duyệt bản tin','Khóa Kịch bản, Ảnh, Giọng và Scene trước khi render.'),stage('wf-output','Render & kết quả','Bản tin, hàng đợi, Render Monitor và video đầu ra.'),stage('wf-settings','Cài đặt','API Key, AI Vision, TTS và trạng thái hệ thống.'));$('wf-settings').classList.add('wf-settings');organize();document.addEventListener('auto-producer-ready',()=>activate('wf-review'));document.addEventListener('review-updated',e=>{if(e.detail?.review?.status==='approved')activate('wf-output')});const wanted=sessionStorage.getItem('autoMediaStage');activate($(wanted)?wanted:'wf-source');let tries=0;const timer=setInterval(()=>{organize();if(++tries>=8)clearInterval(timer)},400)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(mount,250));else setTimeout(mount,250)})();
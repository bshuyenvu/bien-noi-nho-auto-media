export interface VisualMetadata{caption:string;keywords:string[];kind:'photo'|'graphic'|'logo-risk'|'unknown';confidence:number}
const BAD=/\b(logo|icon|avatar|sprite|emoji|banner|advert|ads?|quangcao|tracking|pixel)\b/i;
function clean(s:string){return decodeURIComponent(s).replace(/https?:\/\/[^/]+/i,' ').replace(/\.[a-z0-9]{2,5}(?:\?.*)?$/i,' ').replace(/[-_/+.=?&%0-9]+/g,' ').replace(/\s+/g,' ').trim()}
function words(s:string){return clean(s).toLocaleLowerCase('vi-VN').normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/đ/g,'d').match(/[a-z]{3,}/g)||[]}
export function inferVisualMetadata(url:string,width:number,height:number):VisualMetadata{let raw=url;try{const u=new URL(url);raw=`${u.pathname} ${u.searchParams.get('alt')||''} ${u.searchParams.get('title')||''}`}catch{}const kw=[...new Set(words(raw))].filter(x=>!['image','photo','upload','uploads','media','news','article','content','jpg','jpeg','png','webp'].includes(x)).slice(0,12);const aspect=width/Math.max(1,height);const kind=BAD.test(raw)?'logo-risk':aspect>3.8||aspect<.22?'graphic':'photo';const caption=kw.length?kw.slice(0,7).join(' '):`ảnh ${width}x${height}`;return{caption,keywords:kw,kind,confidence:kw.length>=3?.72:kw.length?.52:.25}}
